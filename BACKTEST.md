# 回測與參數最佳化報告

> **誠實聲明（先讀這段）**
> 本環境**無法連線取得真實 TXF 歷史資料**（券商需登入；逐筆內外盤資料也無免費來源），
> 對外金融資料端點在此沙箱一律被擋（403）。因此下列數字來自**可重現的合成市場
> （Monte-Carlo）**，其用途是：
> 1. 驗證回測引擎與實盤是**同一套程式碼**（非另寫一份）。
> 2. 比較參數的**相對穩健性**、揭露過度最佳化陷阱。
>
> 合成市場**跨日漂移為零均值**，沒有偷塞多頭紅利——這代表它**沒有可被套利的真實邊際**。
> 因此「策略在此資料上不賺錢」是**預期且正確**的結果，**不代表**策略在真實市場必賠，
> 也**不代表**它會賺。真正的邊際只能用**你自己的真實 K 線**驗證（見文末）。

---

## 方法

`backtest.py` 把 tick 餵進與實盤完全相同的元件：
`KBarAggregator → FVDTracker → Strategy → PositionBook → SessionManager`，
並同步重放當日斷路器、單筆停損、收盤強制平倉規則。
成交假設：市價/範圍市價以當下 tick 價 ± 1 點滑價成交，含每邊手續費+稅約 15 元。

評分以**風報比**為核心：恢復係數（淨利/最大回落）、獲利因子 PF、跨種子穩健性。

---

## 關鍵發現

### 1. 單一種子最佳化 = 過度配適（危險示範）

對單一 seed 做 grid search，「冠軍」是 `MA34 / FVD10 / 停損30`（+4,320、PF 1.11）。
但把同一組參數丟到**多個種子**：

| 參數 | 單一種子淨利 | 跨種子中位淨利 | 最差種子 |
|------|------:|------:|------:|
| MA34/FVD10/停損30（單種子冠軍）| **+4,320** | **−6,580** | **−16,870** |

→ 單種子冠軍其實是**跨種子最差**之一。**永遠不要用單一樣本選參數。**

### 2. 趨勢品質濾網 = 真正穩健的風報比改善

新增兩道**非配適**的趨勢濾網（理論上合理，非硬湊數字）：

- **站穩均線緩衝** `entry_ma_buffer_points=3`：收盤需突破 MA 達 3 點才算數，濾掉貼著均線的雜訊穿越。
- **均線斜率濾網** `use_ma_slope_filter=True`：多單只在 MA 上揚時進、空單只在 MA 下彎時進。

跨種子（多 seed × 40~60 日）效果：

| 設定 | 濾網 | 中位淨利 | 最差種子 | 平均筆數 | 平均最大回落 |
|------|:----:|------:|------:|------:|------:|
| MA21/FVD30/停損20 | **off** | −2,740 | **−16,940** | 161 | 6,688 |
| MA21/FVD30/停損20 | **ON**  | −930 | **−3,660** | 94 | 4,865 |
| MA13/FVD30/停損20 | off | −1,940 | −5,700 | 178 | 5,444 |
| MA13/FVD30/停損20 | ON | +90 | −9,210 | 115 | 4,922 |

**濾網把最壞情況從 −16,940 砍到 −3,660、交易次數砍半。**
這是本次最有價值、且**穩健**的改善——降低尾部風險、保護本金，正是你要的「像對待生命般保護資金」。

### 3. 最終採用參數

| 參數 | 值 | 理由 |
|------|----|------|
| `ma_period` | **21** | 尊重你的規格；且跨種子最壞情況（−3,660）優於 MA13（−9,210），更穩。 |
| `fvd_entry_threshold` | 30 | ⚠️ **須依真實 tick 密度重新校準**（見下）。 |
| `per_trade_stop_points` | 20 | ≈200 元/筆（TMF），單日最多 4 次 ≈ 800 元，遠低於 2,890 斷路器。 |
| `entry_ma_buffer_points` | 3 | 站穩均線濾網。 |
| `use_ma_slope_filter` | True | 順勢濾網，砍半雜訊單。 |

最終預設（MA21 + 濾網ON，8 種子 × 60 日）：中位 −930、最差 **−3,660**、平均回落 4,865、
PF 0.94、總勝率 24%、**斷路器 0 次觸發**（單筆停損+強制平倉已把單日虧損壓在 2,890 內）。

> 中位仍略負，是因為**合成市場本無邊際**，成本必然造成微幅淨流出。
> 重點不是這個數字會賺，而是：**風控讓最壞情況可控、尾部不爆倉**。

---

## ⚠️ 上實單前你必須做的事

1. **用真實資料重跑**：由 Shioaji 匯出 5 分 K 存成 CSV
   （欄位 `datetime,Open,High,Low,Close,Volume`），執行：
   ```bash
   python backtest.py --csv your_TXF_5min.csv --symbol TMF --optimize
   ```
   只有真實資料能回答「這策略到底有沒有邊際」。

2. **重新校準 `fvd_entry_threshold`**：FVD 是「視窗內外盤量淨值」，其量級**正比於真實 tick 密度**。
   真實 TXF 每分鐘成交筆數遠高於合成市場，門檻 30 在實盤可能太鬆。
   先用 DRY_RUN 跑一天、觀察 log 裡的 FVD 實際分布，再把門檻設在約其 70 百分位。

3. **CSV 回測的 FVD 是近似值**：K 線無逐筆方向，FVD 由「收盤在 K 棒區間的位置」近似，
   只能粗估。要精準驗證 FVD，需逐筆 tick 歷史（Shioaji `ticks` API 可取近期）。

4. **先模擬主機跑滿一輪早盤+夜盤**，確認濾網、停損、強制平倉、斷路器的實際行為符合預期。

---

## 重現本報告

```bash
# 合成市場 + 最佳化（grid search）
python backtest.py --symbol TMF --days 60 --optimize

# 真實資料
python backtest.py --csv your_data.csv --symbol TMF --optimize
```
參數最佳化與穩健性檢驗函式見 `backtest.py` 的 `optimize()` 與 `robustness()`。
